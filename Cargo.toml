[workspace]
resolver = "3"
members = ["game-model", "game-events", "game-core", "game-main", "game-render"]
exclude = ["game-server"]

[profile.release]
lto = true
opt-level = 's'

[profile.ci-wasm]
inherits = "release"
lto = "thin"
codegen-units = 16